# Names are same as PipelineStep
preprocess:
    sample_size: 1

split_dataset:
    test_size: 0.2
    num_stattest: ks
    num_stattest_threshold: 0.5

select_features:
    metric_diff_threshold: 0.001

feature_engineer:
    kmeans_n_clusters: 4

train:
    n_splits: 5
